@views::view('shared/header.cshtml')
                
                <!-- Breadcrumbs, Page Title -->
                <div id="page-head">
                    <h1><em>add, edit and organize surveys</em>Surveys Publishes</h1>
                    <ul id="breadcrumbs">
                        <li><a href="@mvc::url('home/index')">Home</a></li>
                        <li>Surveys</li>
                    </ul>
                </div>
                
                <!-- Page Content Start -->
                <div class="full-page-text">

                    <p>At as in understood an remarkably solicitude. Mean them very seen she she. Use totally written the observe pressed justice. Instantly cordially far intention recommend estimable yet her his. Ladies stairs enough esteem add fat all enable.</p>

                    <!-- User Details -->
					<div>
						<h3 class="floatleft">Published Surveys</h3>
						<a href="@mvc::url('publish/new')" class="more floatright">Create New</a>
						<div class="clear"></div>
					</div>

                    <table class="extable" width="100%" cellspacing="0" cellpadding="0">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Start Date</th>
								<th>End Date</th>
								<th>Password</th>
								<th>Type</th>
                                <th>Properties</th>
                            </tr>
                        </thead>
                        <tbody>
						@foreach($SurveyPublishs as &$survey) {
                            <tr>
								<td><a href="@mvc::url('surveys/edit/' . $survey['surveyid'])">@$survey['title']</a></td>
                                <td>@statics::datePrint($survey['startdate'], false))</td>
                                <td>@statics::datePrint($survey['enddate'], false)</td>
								<td>@$survey['password']</td>
                                <td>@if($survey['type'] == 1){ Authenticate } else { Public }</td>
                                <td>
									<a href="@mvc::url('publish/edit/' . $survey['surveypublishid'])">Edit</a> |
									<a href="@mvc::url('publish/delete/' . $survey['surveypublishid'])">Delete</a>
								</td>
                            </tr>
						}
                        </tbody>
                    </table>

					<!-- Pagination -->
					<ul id="pagination" class="margintop10px">
						<li><a href="#">&laquo;</a></li>
						<li class="act"><a href="#">1</a></li>
						<li><a href="#">&raquo;</a></li>
					</ul>

				</div>
                <!-- Page Content End -->
    
@views::view('shared/footer.cshtml')